import chalk from 'chalk';

export default function cli(args){
    const messages = {
        "sindi": "Message from Sindi!",
        "Dirk": "Message from Dirk!"
    }
    console.log(chalk.blue('Hello world!'));
}
